import {services} from "@/data/services"; import {categories} from "@/data/categories";
export default async function Services({searchParams}:{searchParams:Promise<{category?:string}>}){const p=await searchParams;const list=p.category?services.filter(s=>s.category===p.category):services;return <main className="container" style={{padding:"32px 0"}}><h1>{p.category?(categories.find(c=>c.id===p.category)?.name||"সেবা"):"সকল সেবা"}</h1><div style={{display:"grid",gap:14,marginTop:20}}>{list.map(s=><a key={s.id} href={"/services/"+s.id} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:16,padding:20,textDecoration:"none",color:"inherit"}}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><strong>{s.name}</strong>{s.verified&&<span style={{color:"#087443",fontSize:13}}>✓ Verified</span>}</div><p style={{margin:"8px 0",color:"#667085"}}>📍 {s.upazila}, {s.district} · {s.area}</p><p style={{margin:0,color:"#475467"}}>{s.description}</p></a>)}</div></main>}